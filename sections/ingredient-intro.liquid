<div class="ingredient-content">
  <div style="background-color: #fff;">
    <div class="row">
      {% if section.settings.image %}
      <!-- <div class="col-sm-5 ingredient-intro-img">
        <img src='{{ section.settings.image.src | img_url: '800x800'}}' />
      </div> -->
      <div class="col-sm-5 col-lg-3 ingredient-intro-img" style="background: url('{{ section.settings.image.src | img_url: '800x800'}}'); background-size: cover;">
        <!-- <img src='{{ section.settings.image.src | img_url: '800x800'}}' /> -->
      </div>
      <div class="col-sm-7 d-flex flex-column align-items-start justify-content-center">
        <div style="padding: 4em 2em;max-width: 600px;">
        {% if section.settings.title %}
        <h2>{{ section.settings.title }}</h2>
        {% endif %}
        <p>{{ section.settings.copy }}</p>
        </div>
      </div>
      {% else %}
      <div class="col-sm-12 d-flex align-items-center justify-content-center">
        <div style="padding: 4em 2em;max-width: 1024px;">
        {% if section.settings.title %}
        <h2>{{ section.settings.title }}</h2>
        {% endif %}
        <p>{{ section.settings.copy }}</p>
        </div>
      </div>
      {% endif %}

    </div>
  </div>
</div>
{% schema %}
    {
        "settings":[
            {
              "type" : "image_picker",
              "id": "image",
              "label": "image - if set this will appear"
            },
            {
              "type": "text",
              "id": "title",
              "label": "Section Title - optional"
            },
            {
              "type": "richtext",
              "id": "copy",
              "label": "Section Copy",
              "default": "<p>We don’t believe in the power of plants – we know, first-hand, how powerful they are. Pangea Organics has extracted the world’s finest botanical actives by utilizing the latest scientific methods to create a line of Skin, Lip, and Bodycare that is unsurpassed in its efficacy, elegance, and ethos. This all-star lineup of active, plant-based ingredients is what makes our products such glow-enhancing, anti-aging powerhouses in your self-care ritual.</p>"
            }
        ]
    }

{% endschema %}
