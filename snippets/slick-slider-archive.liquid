  {% comment %} <div class="slideshow-wrapper slider single-item">
    <button type="button" class="visually-hidden slideshow__pause" data-id="{{ section.id }}" aria-live="polite">
      <span class="slideshow__pause-stop">
        {% include 'icon-pause' %}
        <span class="icon__fallback-text">{{ 'sections.slideshow.pause_slideshow' | t }}</span>
      </span>
      <span class="slideshow__pause-play">
        {% include 'icon-play' %}
        <span class="icon__fallback-text">{{ 'sections.slideshow.play_slideshow' | t }}</span>
      </span>
    </button>

    <div class="slideshow header-slideshow slideshow--{{ section.settings.slideshow_height }}" id="Slideshow-{{ section.id }}" data-autoplay="{{ section.settings.autoplay }}" data-speed="{{ section.settings.autoplay_speed }}">

                {% for block in section.blocks %}

        <div class=" slideshow__slide slideshow__slide--{{ block.id }} {% if forloop.first %}slick-active{% endif %}{% if is_background_video %} slideshow__slide--background-video{% endif %}" {{ block.shopify_attributes }}>
                  {%- assign is_background_video = false -%}
                  {% if block.type == 'video' %}
                    {% if block.settings.video_type == 'background' or block.settings.video_type =='background-chrome' %}
                      {%- assign is_background_video = true -%}
                    {% endif %}
                  {% endif %}
            <div class="slideshow__image box ratio-container lazyload{% unless forloop.first == true %} lazypreload{% endunless %} js"
                 data-bgset="{% include 'bgset', image: block.settings.image %}"
                 data-sizes="auto"
                 data-parent-fit="cover"
                 style="background-position: {{ block.settings.alignment }};{% if forloop.first == true %} background-image: url('{{ block.settings.image | img_url: '1200x1200' }});{% endif %}">
            </div>
          <div class="slideshow__text-wrap{% if block.settings.title != blank or block.settings.subheading != blank %} slideshow__overlay{% endif %} " style="">
            <div class="overlay-box" style="background-color: {{ block.settings.color_image_overlay }}; opacity: {{ block.settings.color_image_opacity | times: 0.01 }};">

            </div>

            <div class="slideshow__text-content {% if block.settings.justification == 'left' %}left{% elsif block.settings.justification == 'right' %}right{% endif %} {% if forloop.first %}slick-active{% endif %}">
              <div class="page-width">
                {% unless block.settings.title == blank %}
                  <h2 class="h1 mega-title slideshow__title{% if settings.link != blank %} slideshow__title--has-link{% endif %}{% if section.settings.text_size == 'large' %} mega-title--large{% endif %}">
                    {{ block.settings.title | escape }}
                  </h2>
                {% endunless %}
                {% unless block.settings.subheading == blank %}
                  <span class="mega-subtitle slideshow__subtitle{% if section.settings.text_size == 'large' %} mega-subtitle--large{% endif %}">
                    {{ block.settings.subheading | escape }}
                  </span>
                {% endunless %}
                {% unless block.settings.link == blank %}
                <a type="submit" name="header-cta" class="btn btn-slider" href="{{block.settings.link}}"><span>{{ block.settings.cta_text }}</span></a>
                {% endunless %}
                {% if block.type == 'video' %}
                  {% unless block.settings.video_type == 'background' %}
                    <div class="slideshow__video-control--play-wrapper{% if block.settings.title != blank or block.settings.subheading != blank %} slideshow__video-control--play-wrapper--push{% endif %}">
                      <button type="button" class="text-link slideshow__video-control slideshow__video-control--play" data-controls="SlideshowVideo-{{ block.id }}">
                        {% include 'icon-play-video' %}
                        <span class="icon__fallback-text">{{ 'sections.slideshow.play_video' | t }}</span>
                      </button>
                    </div>
                  {% endunless %}
                {% endif %}
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div> {% endcomment %}

{% comment %} $('.header-slideshow').slick({
        autoplay: true,
        // fade: true,
        speed: 1000,
        autoplaySpeed: 5000,
        slidesToShow: 1,
        centerMode: true,
        centerPadding: "0px",
        adaptiveHeight: false,
        variableWidth: false
      }); {% endcomment %}


{% comment %} ALPINE {% endcomment %}

      {% comment %} <div class="slideshow-wrapper slider single-item">
      <button type="button" class="visually-hidden slideshow__pause" data-id="{{ section.id }}" aria-live="polite">
        <span class="slideshow__pause-stop">
          {% include 'icon-pause' %}
          <span class="icon__fallback-text">{{ 'sections.slideshow.pause_slideshow' | t }}</span>
        </span>
        <span class="slideshow__pause-play">
          {% include 'icon-play' %}
          <span class="icon__fallback-text">{{ 'sections.slideshow.play_slideshow' | t }}</span>
        </span>
      </button>

      <div class="slideshow header-slideshow slideshow--{{ section.settings.slideshow_height }}" id="Slideshow-{{ section.id }}" data-autoplay="{{ section.settings.autoplay }}" data-speed="{{ section.settings.autoplay_speed }}">

          {% for block in section.blocks %}
          {%- assign slider_product = all_products[block.settings.slider_product] -%}
          {%- assign current_variant_1 = slider_product.selected_or_first_available_variant -%}
          {%- assign featured_image_1 = current_variant_1.featured_image | default: slider_product.featured_image -%}
          {% if block.settings.slider_product_image != blank %}
          {% assign custom_image = block.settings.slider_product_image.src %}
          {% endif %}
          <div class=" slideshow__slide slideshow__slide--{{ block.id }} {% if forloop.first %}slick-active{% endif %}{% if is_background_video %} slideshow__slide--background-video{% endif %}" {{ block.shopify_attributes }}>
                    {%- assign is_background_video = false -%}
                    {% if block.type == 'video' %}
                      {% if block.settings.video_type == 'background' or block.settings.video_type =='background-chrome' %}
                        {%- assign is_background_video = true -%}
                      {% endif %}
                    {% endif %}
              <div class="slideshow__image box ratio-container lazyload{% unless forloop.first == true %} lazypreload{% endunless %} js"
                   data-bgset="{% include 'bgset', image: block.settings.image %}"
                   data-sizes="auto"
                   data-parent-fit="cover"
                   style="background-position: {{ block.settings.alignment }};{% if forloop.first == true %} background-image: url('{{ block.settings.image | img_url: '300x300' }});{% endif %}">
              </div>
            <div class="slideshow__text-wrap{% if block.settings.title != blank or block.settings.subheading != blank %} slideshow__overlay{% endif %} " style="">
              <div class="overlay-box" style="background-color: {{ block.settings.color_image_overlay }}; opacity: {{ block.settings.color_image_opacity | times: 0.01 }};">

              </div>

              <div class="slideshow__text-content {% if block.settings.justification == 'left' %}left{% elsif block.settings.justification == 'right' %}right{% endif %} {% if forloop.first %}slick-active{% endif %}">
                <div class="row d-flex align-items-center header-feature contained">
                  <div class="col-md-5 col-lg-4 offset-md-0 order-2 order-md-1 alpine-text-box">
                    <div class="header-title">
                        <h2 class="collection-title">
                          {{ collection.handle | replace: '-', ' ' }}
                        </h2>
                      {% unless block.settings.subheading == blank %}
                        <h3 class="slideshow__subtitle">
                          {{ block.settings.subheading | escape }}
                        </h3>
                      {% endunless %}
                      {% unless block.settings.cta_text == blank %}
                      <a type="submit" name="header-cta" class="btn" href="{{ slider_product.url}}"><span>{{ block.settings.cta_text }}</span></a>
                      {% endunless %}
                    </div>
                  </div>
                  <div class="col-md-7 col-lg-4 order-1 order-md-2 text-center">
                    <div class="header-feature-wrap">
                      <a href="{{ slider_product.url }}">
                      {% if block.settings.slider_product_image != blank %}
                      {% include 'responsive-image' with
                          image: custom_image,
                          max_width: 700,
                          max_height: 800
                        %}
                      {% else %}
                        {% include 'responsive-image' with
                          image: featured_image_1,
                          max_width: 700,
                          max_height: 800
                        %}
                      {% endif %}
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div> {% endcomment %}